<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="12dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_swatch"
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:cardBackgroundColor="@color/notebook_cream"
        app:cardCornerRadius="14dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/notebook_line"
        app:strokeWidth="1.5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/image_swatch_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_pencil"
            app:tint="@color/notebook_ink_soft"
            tools:ignore="ContentDescription" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/text_swatch_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="Classic"
        android:textColor="@color/notebook_ink"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/card_swatch"
        app:layout_constraintStart_toStartOf="@id/card_swatch"
        app:layout_constraintEnd_toEndOf="@id/card_swatch" />

</androidx.constraintlayout.widget.ConstraintLayout>